<p id="notice"><%= notice %></p>

<h1>Hist Precios</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hist_precios.each do |hist_precio| %>
      <tr>
        <td><%= link_to 'Show', hist_precio %></td>
        <td><%= link_to 'Edit', edit_hist_precio_path(hist_precio) %></td>
        <td><%= link_to 'Destroy', hist_precio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Hist Precio', new_hist_precio_path %>

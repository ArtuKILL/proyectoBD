<%= form_with(model: rally) do |form| %>
  <% if rally.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rally.errors.count, "error") %> prohibited this rally from being saved:</h2>

      <ul>
        <% rally.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: itinerario_atraccion) do |form| %>
  <% if itinerario_atraccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(itinerario_atraccion.errors.count, "error") %> prohibited this itinerario_atraccion from being saved:</h2>

      <ul>
        <% itinerario_atraccion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
